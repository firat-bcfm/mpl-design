@Library('mpl') _

/**
 * Ozkan Development Pipeline
 * Uses common company-wide settings
 */

OzkanDevPipeline {
    // Proje özeli
    projectName = 'ozkan-app'
    customMessage = 'özkan edge blue içer'

    // ORTAK AYARLAR (Firat ile aynı)
    slackChannel = '#team-deployments'
    dockerRegistry = 'docker.io/company'
    grafanaUrl = 'https://grafana.ozkandev.com'
    minTestCoverage = '80%'
    showGitInfo = true

    // TRIVY: Otomatik preset config kullanılır (enabled=true, scanType='image', severity='MEDIUM,HIGH,CRITICAL')
    // Override için: 'trivy.enabled' = false, 'trivy.severity' = 'CRITICAL' gibi değerler ekle
}
