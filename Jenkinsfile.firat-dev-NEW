@Library('mpl') _

/**
 * Firat Development Pipeline - SIMPLIFIED VERSION
 */

FiratDevPipeline {
    // Proje özeli
    projectName = 'firat-app'
    customMessage = 'Firat deployment başarılı!'

    // ORTAK AYARLAR
    slackChannel = '#team-deployments'
    dockerRegistry = 'docker.io/company'
    grafanaUrl = 'https://grafana.company.com'
    minTestCoverage = '80%'
    showGitInfo = true

    // TRIVY - YENİ BASIT YOL
    // Artık CommonTrivyConfig kullanarak tek satırda!
    // Arka planda: fs scan, HIGH/CRITICAL, table format
}
